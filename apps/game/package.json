{
  "name": "@blazon/game",
  "version": "0.0.0",
  "private": true,
  "description": "Blazon Heraldry Quiz — multi-language quiz game for Polish coats of arms",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@blazon/core": "workspace:*",
    "@blazon/poland": "workspace:*",
    "@blazon/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "rimraf": "catalog:"
  }
}
